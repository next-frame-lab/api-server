plugins {
    id 'checkstyle'
}

group = 'wisoft'
version = '0.0.1-SNAPSHOT'


repositories {
    mavenCentral()
}

dependencies {
}

checkstyle {
    maxWarnings = 0
    configFile = file("${rootDir}/config/checkstyle/naver-checkstyle-rules.xml")
    toolVersion = "10.25.0"
}